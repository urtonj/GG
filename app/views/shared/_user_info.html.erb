<div class="user_info">
	<a href="<%= user_path(current_user) %>">
	<%= gravatar_for current_user, :size => 30 %>
	<span class="user_name">
		<%= current_user.name %>'s list
	</span><br />
	<span class="lists">
		<%= pluralize(current_user.lists.count, "item") %>
	</span>
</div>